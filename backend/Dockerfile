# Stub — a API ainda não foi implementada nesta etapa do refactor.
# Quando houver código, este Dockerfile deve construir a API (ex.: Node/Express)
# e expor a porta 3001 consumida pelo frontend.
#
# Exemplo de ponto de partida:
#
# FROM node:22-alpine
# WORKDIR /app
# COPY package.json package-lock.json ./
# RUN npm ci
# COPY . .
# EXPOSE 3001
# CMD ["npm", "run", "start"]

FROM node:22-alpine
WORKDIR /app
CMD ["sh", "-c", "echo 'backend ainda não implementado' && sleep infinity"]
